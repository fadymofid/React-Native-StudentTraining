{/* <ImageBackground source={require("./assets/travel-1800268.jpg")} style={styles.backgroundImage}>
    <SectionList style={styles.container}
      sections={users}
      renderItem={({item}) => (
        <View style={styles.userItem}>
         <Image source={require("./assets/free-user-avatar-contact-portfolio-personal-portrait-profile-2-5270.png")} style={styles.userAvatar}></Image>
        <View>
        <Text style={styles.userName}>{item.name}</Text>
        </View>
      
        </View>
      )}
      renderSectionHeader={({ section }) => (
        <Text style={styles.sectionHeader}>{section.title}</Text>
    
      )}
    />
    </ImageBackground> */}
